{
  "name": "pagebraid-mcp",
  "version": "0.5.1",
  "description": "A local PDF MCP server with budget-first multimodal PDF reading.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Victor-Quqi/pagebraid-mcp.git"
  },
  "homepage": "https://github.com/Victor-Quqi/pagebraid-mcp#readme",
  "bugs": {
    "url": "https://github.com/Victor-Quqi/pagebraid-mcp/issues"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "pdf",
    "pdf-reader",
    "multimodal",
    "agent",
    "codex"
  ],
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pagebraid-mcp": "dist/index.js",
    "pagebraid-debug": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "tsc -p tsconfig.json --noEmit && node scripts/build.mjs",
    "check": "npm run build",
    "check:package": "npm run check && node scripts/check-package.mjs",
    "prepack": "npm run build",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.80"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@types/node": "^22.13.10",
    "esbuild": "0.28.2",
    "js-tiktoken": "1.0.21",
    "pdfjs-dist": "5.6.205",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "zod": "^4.5.4"
  }
}
